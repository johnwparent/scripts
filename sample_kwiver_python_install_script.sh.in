#! /usr/bin/sh

# we also need to consider where the python bindings are ending up/being installed to
# particularly in the event the install option is actually provided - i.e. we should only be building to
# a venv's site packages of a venv is desired
# how do we configure that?

# also how do we integrate the install step of a potential out of the rest of kwiver install of the kwiver python bindings

@PYTHON_EXECUTABLE@ -m pip install @REQUIREMENTS_FILE@
